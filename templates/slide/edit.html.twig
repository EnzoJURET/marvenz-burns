{% extends 'base.html.twig' %}

{% block title %}Edit Slide{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>
<div class="example-wrapper">
    <h1 class="text-center">Editer une Slide</h1>
</div>
<div class="col-sm-8 offset-2">
    <hr>
</div>
<div class="col-sm-8 offset-2">

    {{ include('slide/_form.html.twig', {'button_label': 'Enregistrer'}) }}

    <a href="{{ path('slide_index') }}">Retour a la liste</a>

    {{ include('slide/_delete_form.html.twig') }}
    <form enctype="multipart/form-data" action="" method="post">
      <input type="hidden" name="MAX_FILE_SIZE" value="100000" />
        Transfère le fichier
      <input type="file" name="monfichier" />
      <input type="submit" />
    </form>
    <?php uploadImage() ?>

</div>
{% endblock %}
